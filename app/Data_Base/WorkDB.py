# from connectDB import Get_engine_session_DB
# from model import Foto

# connectDB = Get_engine_session_DB('base_foto.db')
# engine = connectDB.get_engine()
# session = connectDB.get_sessoin()


# foto = Foto(name_foto=name_foto, path=path)
# session.add(foto)
# session.commit()
