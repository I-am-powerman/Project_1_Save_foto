# Project_1_Save_foto

Сохранение фото с iPhone через веб-интерфейс.

## 📋 Описание

Проект представляет собой веб-приложение для загрузки и сохранения фотографий с iPhone на сервер.
Включает в себя:
- **FastAPI** сервер для загрузки файлов
- **SQLite** базу данных для хранения метаданных
- **Tkinter GUI** для выбора диска сохранения

## 🚀 Быстрый старт

### 1. Установка системных зависимостей

**Tkinter** (требуется для GUI):

```bash
# Ubuntu/Debian
sudo apt-get install -y python3-tk

# Fedora
sudo dnf install python3-tkinter

# Arch Linux
sudo pacman -S tk
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка (опционально)

Скопируйте `.env.example` в `.env` и при необходимости измените настройки:

```bash
cp .env.example .env
```

### 5. Запуск приложения

```bash
python3 main.py
```

**Порядок работы:**

1. Откроется окно GUI
2. Выберите диск для сохранения файлов из списка
3. Закройте окно GUI
4. **Автоматически запустится веб-сервер** на порту 8000
5. Откройте браузер: `http://localhost:8000` или `http://<ваш-IP>:8000`
6. Введите имя папки и загрузите файлы

> **Примечание:** Если выбранный диск недоступен или нет прав на запись, сервер будет использовать папку `uploads` в проекте.

### Альтернативный запуск сервера (без GUI)

```bash
uvicorn app.web_interface.web:app --host 0.0.0.0 --port 8000 --reload
```

## 📁 Структура проекта

```
Project_1_Save_foto/
├── app/
│   ├── Data_Base/
│   │   ├── __init__.py
│   │   ├── connectDB.py      # Подключение к БД
│   │   ├── model.py          # SQLAlchemy модели
│   │   └── WorkDB.py         # Класс для работы с БД
│   ├── gui/
│   │   ├── __init__.py
│   │   ├── Gui.py            # Tkinter интерфейс
│   │   └── functions/
│   │       ├── get_name_disk.py
│   │       └── loc_ip.py
│   └── web_interface/
│       ├── __init__.py
│       ├── web.py            # FastAPI приложение
│       └── index.html        # Веб-интерфейс
├── uploads/                   # Папка для загруженных файлов (по умолчанию)
├── .env                       # Переменные окружения
├── .env.example              # Пример переменных окружения
├── main.py                    # Точка входа (GUI + сервер)
├── requirements.txt           # Зависимости
├── setup.sh                   # Скрипт автоматической установки
└── README.md                  # Документация
```

## 🔧 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/` | Главная страница загрузки |
| POST | `/upload` | Загрузка файлов |
| GET | `/health` | Проверка работоспособности |

### Пример загрузки файлов

```bash
curl -X POST "http://localhost:8000/upload" \
  -F "files=@photo1.jpg" \
  -F "files=@photo2.jpg" \
  -F "name_dir=my_vacation"
```

## 🗂 Как работает сохранение файлов

```
Путь сохранения = /media/{пользователь}/{выбранный_диск}/{имя_папки}/

Пример:
  GUI: выбран диск "USB_Disk"
  Веб-интерфейс: имя папки "vacation_2024"
  Итоговый путь: /media/sky/USB_Disk/vacation_2024/
```

Если диск недоступен → используется `./uploads/`

## 🛠 Исправления и улучшения

В ходе доработки были исправлены следующие ошибки:

1. **connectDB.py** — исправлена опечатка `get_sessoin` → `get_session`, добавлено кэширование engine/session_factory
2. **model.py** — тип поля `path` изменён с `Integer` на `String(512)`, добавлен `__repr__`
3. **WorkDB.py** — создан полноценный класс для работы с БД (CRUD операции)
4. **web.py** — исправлены пути, добавлена инициализация БД через WorkDB, async/await
5. **get_name_disk.py** — добавлена поддержка Windows и macOS
6. **Gui.py** — обновлены импорты, добавлен автозапуск сервера после закрытия окна, выбор диска
7. Добавлены `__init__.py` файлы для пакетов
8. Создан `.env` файл с настройками

## 📝 Лицензия

MIT
